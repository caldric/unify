interface Category {
  item: string;
  section: string;
}

const categories: Category[] = [
  { item: 'chicken', section: 'meat' },
  { item: 'carrot', section: 'produce' },
];

export { categories };
